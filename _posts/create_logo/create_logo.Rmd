---
title: "Generating a logo"
description: |
  Generating a logo for this site in R.
author:
  - name: Jannik Buhr 
    url: https://jmbuhr.de
date: 09-07-2020
output:
  distill::distill_article:
    self_contained: false
categories:
  - misc
---

We need a favicon for this site.
So why not do it in R straight away?

```{r generate-logo, echo=TRUE, fig.width=5, fig.height=5, dev.args=list(bg="transparent")}
library(tidyverse)

df <- hilbeRt::get_hilbert_paths(4)
plt <- df %>%
  ggplot(aes(x, y, color = index)) +
  geom_path(size = 5, show.legend = FALSE, lineend = "round") +
  theme_void() +
  coord_equal() +
  scale_color_gradient2(low = "#002ab5",
                        mid = "#8a00b5",
                        high = "#002ab5")
plt
ggsave(here::here("images/favicon.png"), bg = "transparent")
```
